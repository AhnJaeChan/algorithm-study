#include <iostream>
#include <string>

using namespace std;

inline int max(int a, int b) {
    return a > b ? a : b;
}

int main() {
    int N;

    cin >> N;

    int T[N], P[N];
    int F[N];

    for (int i = 0; i < N; ++i) {
        cin >> T[i] >> P[i];
    }



    return 0;
}
