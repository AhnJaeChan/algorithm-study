#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;

string a;

int cache[10000][3];

int def(int start) {

}

int main() {
    int T;
    cin >> T;
    while (T--) {
        cin >> a;
        for (int i = 0; i < a.size(); i++)
            fill_n(cache, 3, -1);



    }
    return 0;
}